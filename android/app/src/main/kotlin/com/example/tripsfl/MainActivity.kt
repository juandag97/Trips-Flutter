package com.example.tripsfl

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // this.getWindow().setStatusBarColor(android.graphics.Color.TRANSPARENT);
}
